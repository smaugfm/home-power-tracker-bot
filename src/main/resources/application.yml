spring:
  main:
    banner-mode: off
  liquibase:
    change-log: liquibase.yml
    default-schema: public
    liquibase-schema: public
  r2dbc:
    pool:
      initial-size: 5
      max-size: 10
app:
  loop:
    interval: 10s
    reachable-timeout: 10s
  network-stability:
    interval: 600ms
    timeout: 1s
    wait-for-stable-network-timeout: 1h
    consecutive-tries-to-consider-online: 20
    hosts:
      - 188.190.254.254
      - 1.1.1.1
      - 8.8.8.8
