spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: postgres
    password: postgres
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

logging:
  level:
    liquibase: INFO
